# Backend Integration

Before a live adapter is enabled, confirm actual Node.js schemas, authentication, errors, pagination, roles, and ownership rules. Map those contracts in services while preserving the UI domain models.
